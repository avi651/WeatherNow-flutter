/// The unit the app displays temperatures in. Weather data is always
/// fetched and cached in Celsius (see [CurrentWeather.temperatureCelsius]);
/// this only governs presentation, converted at the point of display.
enum TemperatureUnit { celsius, fahrenheit }
